{
  "name": "vroomy",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2",
    "prettier": "^3.8.1"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx}": "bunx eslint --config frontend/eslint.config.js",
    "**/*.{js,jsx,ts,tsx,css,json,md}": "bunx prettier --write"
  },
  "private": true,
  "scripts": {
    "prepare": "husky || true",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
